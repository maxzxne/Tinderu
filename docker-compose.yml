services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      DATA_DIR: /data
    volumes:
      - tinderu_data:/data
    restart: unless-stopped

volumes:
  tinderu_data:
