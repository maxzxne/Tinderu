services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      DATA_DIR: /app/data
    volumes:
      - tinderu_data:/app/data
    restart: unless-stopped

volumes:
  tinderu_data:
